name: workflow
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Установить PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '7.1'
      # - name: Запустить тесты
      #   run: vendor/bin/phpunit
      - name: Деплой на сервер
        uses: easingthemes/ssh-deploy@v2.2.9
        with:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_ONLINE_QUIZ }}
          REMOTE_HOST: '185.114.245.100'
          REMOTE_USER: 'cn73246'
          SOURCE: 'C:\OpenServer\domains\online-quiz'
          TARGET: '/home/REMOTE_USER/online-quiz/public_html'

